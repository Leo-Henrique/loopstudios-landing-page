import styled, { css } from "styled-components";

export const Wrapper = styled.section(({ theme }) => (css`
    
`));

export const Intro = styled.div(({ theme }) => (css`
    
`));

export const Collections = styled.ul(({ theme }) => (css`
    
`));